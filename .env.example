# PostgreSQL Database Configuration (for Render)
# Get these values from your Render PostgreSQL database dashboard

# Database URL from Render (format: postgresql://user:password@host:port/database)
DB_URL=postgresql://user:password@your-render-db-host.render.com:5432/railway_db

# Database username
DB_USERNAME=your_db_user

# Database password
DB_PASSWORD=your_db_password

# Optional: Kafka configuration if using Render for Kafka too
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
