server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://dpg-d6a35an5r7bs73fgs510-a.oregon-postgres.render.com/railway_v3pl
    username: railway_v3pl_user
    password: cWUU8smbe7Ig2jJwmIzju70neBnW7BCN
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      group-id: railway-group
      auto-offset-reset: earliest
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.LongSerializer

jwt:
  secret: "supersecretkeyforjwt"

logging:
  level:
    org.springframework: INFO
    com.railway.backend: DEBUG
