server:
  port: 8081

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-d6a35an5r7bs73fgs510-a.oregon-postgres.render.com:5432/railway_v3pl?sslmode=require&connectTimeout=10&socketTimeout=30&tcpKeepAlive=true}
    username: ${DB_USERNAME:railway_v3pl_user}
    password: ${DB_PASSWORD:cWUU8smbe7Ig2jJwmIzju70neBnW7BCN}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 8
      minimum-idle: 2
      connection-timeout: 20000
      validation-timeout: 5000
      idle-timeout: 120000
      max-lifetime: 240000
      keepalive-time: 60000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: railway-group
      auto-offset-reset: earliest
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.LongSerializer

jwt:
  secret: "supersecretkeyforjwt"

app:
  kafka:
    enabled: ${APP_KAFKA_ENABLED:false}
  ai:
    enabled: ${APP_AI_ENABLED:true}
    classifier-url: ${APP_AI_CLASSIFIER_URL:https://midhun-2542-railwaymodel.hf.space/classify}
  officer-signup-key: ${APP_OFFICER_SIGNUP_KEY:RAILMADAD_OFFICER_2026}

logging:
  level:
    org.springframework: INFO
    com.railway.backend: DEBUG
